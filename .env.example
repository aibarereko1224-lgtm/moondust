# Public Supabase client configuration.
# Add real values only to a local .env.local file or the Netlify dashboard.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Server-only. Use this later in Netlify environment variables, never VITE_*.
TMDB_API_KEY=
